# Отладка проблемы с отображением подписок на подборки

**Дата**: 2026-01-15 00:50  
**Пользователь**: telegram_id = 139883458 (ilyasni)  
**Context7**: Отладка проблемы - API возвращает данные, но бот не видит подписки

---

## Проблема

**Симптомы**:
- ✅ API `/api/themes/users/139883458/subscribed` возвращает 200 OK с 2 подписками
- ❌ Бот показывает "У вас пока нет подключенных подборок"
- ❌ Пользователь не видит свои подписки

---

## Проверка данных

**API ответ**:
```json
{
    "themes": [
        {
            "theme_id": "89629f48-8790-4163-b972-88d86c7f830d",
            "theme_slug": "diy-mens",
            "theme_name": "DIY для мужчин",
            "subscribed_at": "2026-01-14T10:28:21.237272+00:00",
            "is_active": true
        },
        {
            "theme_id": "da547c68-a339-451b-a60d-d683b1fe4d34",
            "theme_slug": "startup",
            "theme_name": "Стартапы",
            "subscribed_at": "2026-01-14T06:38:30.730247+00:00",
            "is_active": true
        }
    ],
    "total": 2
}
```

**БД проверка**:
- ✅ 2 активные подписки для пользователя `cc1e70c9-9058-4fd0-9b52-94012623f0e0`

---

## Анализ кода

**Обработчик**: `api/bot/handlers/themes_handlers.py:240-293`

**Логика**:
1. Запрос к API: `GET /api/themes/users/{msg.from_user.id}/subscribed`
2. Проверка статуса: `if resp.status_code == 200:`
3. Парсинг JSON: `data = resp.json()`
4. Извлечение themes: `themes = data.get("themes", [])`
5. Проверка: `if not themes:` → показывает "нет подписок"

**Проблема**: API возвращает данные, но `themes` оказывается пустым списком

---

## Исправление

### Добавлено логирование

**Файл**: `api/bot/handlers/themes_handlers.py:252-260`

**Изменения**:
- ✅ Добавлено логирование ответа API
- ✅ Логирование количества themes
- ✅ Логирование структуры данных

**Код**:
```python
logger.info(
    "My themes API response",
    user_id=msg.from_user.id,
    status_code=resp.status_code,
    themes_count=len(themes),
    themes_data=themes if len(themes) <= 2 else f"{len(themes)} themes",
    raw_data_keys=list(data.keys()) if data else []
)
```

---

## Следующие шаги

1. ✅ **Перезапущен API** с логированием
2. ⏳ **Попросить пользователя** использовать `/my_themes` снова
3. ⏳ **Проверить логи** для понимания, что происходит с данными
4. ⏳ **Исправить проблему** на основе логов

---

## Возможные причины

1. **Проблема с парсингом JSON** - исключение при `resp.json()`
2. **Проблема с форматом данных** - структура ответа не соответствует ожидаемой
3. **Проблема с кодировкой** - Unicode символы в названиях
4. **Исключение в коде** - ошибка до проверки `if not themes:`

---

## Рекомендации

1. ✅ **Проверить логи** при следующем использовании `/my_themes`
2. ✅ **Убедиться**, что исключения логируются
3. ✅ **Проверить**, что `resp.json()` не выбрасывает исключение
