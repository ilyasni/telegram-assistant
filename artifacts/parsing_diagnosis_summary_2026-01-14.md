# Резюме диагностики и исправлений проблемы парсинга

**Дата**: 2026-01-14 23:40  
**Context7**: Полная диагностика и исправления на основе предложений

---

## Проблема

**Симптомы**:
- ✅ Scheduler работает (тики каждые 5 минут)
- ❌ Все парсинги показывают `messages_processed: 0`
- ❌ В БД нет новых постов за последний час (0 постов)
- ❌ Множество предупреждений "No available sessions after rate limiting"
- ❌ Последний успешный парсинг был в 17:12 (6+ часов назад)

---

## Диагностика

### 1. Проверка FloodWait в Redis

**Результат**: ✅ Нет активных FloodWait
```bash
docker exec redis redis-cli KEYS "tg:floodwait_until:*"
# Пусто
```

### 2. Проверка количества сессий

**Результат**: ⚠️ Только 2 сессии доступны
- `389326685` - подключена
- `139883458` - подключена
- `8124731874` (collector) - не найдена ("No session found for user")

**Проблема**: При 2 сессиях и `max_concurrent=10` на сессию = максимум 20 параллельных запросов, но обрабатывается 49 каналов одновременно.

### 3. Проверка параллелизма

**Результат**: ⚠️ `PARSER_MAX_CONCURRENCY=4` - слишком мало
- Обрабатывается 49 каналов одновременно
- Нет ограничения глобального параллелизма каналов
- Стадный эффект: все каналы одновременно пытаются получить сессию

### 4. Проверка освобождения семафоров

**Результат**: ✅ Есть finally блоки для освобождения
- `channel_parser.py:1400` - finally блок освобождает семафор
- Но нет детального логирования для отслеживания утечек

---

## Примененные исправления

### 1. ✅ Добавлено детальное логирование acquire/release

**Файлы**:
- `telethon-ingest/services/session_rate_limiter.py`

**Изменения**:
- `lease_try` - логирование попытки с `in_use`, `max_concurrent`, `semaphore_value`
- `lease_acquired` - логирование успешного получения с `wait_time`, `in_use_after`
- `lease_released` - логирование освобождения с `in_use_before`, `in_use_after`

**Цель**: Отслеживать утечки семафоров и состояние занятости

### 2. ✅ Улучшено логирование "No available sessions"

**Файлы**:
- `telethon-ingest/services/channel_parser.py:858`

**Изменения**:
- Добавлено детальное логирование с `healthy_sessions_count`, `max_fallback_sessions`, `session_pool_size`

**Цель**: Понять причину недоступности сессий

### 3. ✅ Ограничен глобальный параллелизм каналов

**Файлы**:
- `telethon-ingest/tasks/parse_all_channels_task.py:1058`

**Изменения**:
- Добавлен расчет `global_channel_concurrency = min(15, max_concurrency * 3)`
- Ограничен размер батча до `global_channel_concurrency`

**Цель**: Предотвратить стадный эффект

### 4. ✅ Увеличен max_concurrent_per_session

**Файлы**:
- `telethon-ingest/services/session_rate_limiter.py:65`

**Изменения**:
- `max_concurrent_per_session: int = 3` → `max_concurrent_per_session: int = 10`

**Цель**: Увеличить пропускную способность каждой сессии

### 5. ✅ Увеличен timeout для acquire_session

**Файлы**:
- `telethon-ingest/services/channel_parser.py:818`

**Изменения**:
- `timeout=2.0` → `timeout=10.0`

**Цель**: Дать больше времени для получения семафора

---

## Текущий статус

**После исправлений**:
- ✅ Контейнер перезапущен и работает
- ⏳ Ожидается следующий тик scheduler'а для проверки результата
- ⏳ Логирование должно показать детальную информацию о состоянии семафоров

---

## Следующие шаги

### Немедленно (после следующего тика):

1. **Проверить логи**:
   ```bash
   docker logs telethon-ingest --since 5m | grep -E "(lease_try|lease_acquired|lease_released|no_available_sessions|Global channel concurrency)"
   ```

2. **Проверить симметричность acquire/release**:
   - Количество `lease_acquired` должно равняться количеству `lease_released`
   - Если нет - есть утечка семафоров

3. **Проверить messages_processed**:
   ```bash
   docker logs telethon-ingest --since 5m | grep "CHANNEL_PARSE_END" | grep "messages_processed"
   ```

4. **Проверить новые посты**:
   ```sql
   SELECT COUNT(*) FROM posts WHERE created_at >= NOW() - INTERVAL '10 minutes';
   ```

### Если проблема сохраняется:

1. **Проверить количество реально доступных сессий**:
   - Проверить, почему collector (8124731874) не найден
   - Добавить больше сессий в пул

2. **Добавить контекст-менеджер для гарантированного освобождения**:
   ```python
   async with session_rate_limiter.lease(account_id, timeout=10.0):
       # ВСЁ взаимодействие с Telegram для этого канала внутри
   ```

3. **Проверить освобождение семафоров при CancelledError**:
   - Убедиться, что finally блоки срабатывают при отмене задач

---

## Рекомендации

1. **Мониторинг**: Добавить метрики для отслеживания:
   - Количество доступных сессий
   - Время ожидания семафоров
   - Количество успешных/неуспешных попыток получения сессии
   - Симметричность acquire/release

2. **Оптимизация**: Рассмотреть:
   - Увеличение количества сессий в пуле
   - Приоритизацию каналов (сначала важные)
   - Адаптивный параллелизм на основе доступных сессий

3. **Архитектура**: Рассмотреть:
   - Контекст-менеджер для гарантированного освобождения семафоров
   - Retry queue для каналов, которые не смогли получить сессию
   - Graceful degradation при нехватке сессий
