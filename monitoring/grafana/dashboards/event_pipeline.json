{
  "annotations": {
    "list": []
  },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "iteration": 1,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "datasource": "Prometheus",
      "fieldConfig": {
        "defaults": {
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              { "color": "green", "value": null },
              { "color": "red", "value": 1 }
            ]
          },
          "unit": "none"
        },
        "overrides": []
      },
      "gridPos": { "h": 6, "w": 8, "x": 0, "y": 0 },
      "id": 1,
      "options": {
        "legend": { "calcs": ["last"], "displayMode": "list", "placement": "right" },
        "tooltip": { "mode": "single" }
      },
      "targets": [
        {
          "expr": "sum by(stream, group) (stream_pending_size)",
          "legendFormat": "{{stream}}/{{group}}",
          "refId": "A"
        }
      ],
      "title": "Pending в Redis Streams",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": {
        "defaults": { "unit": "ops" },
        "overrides": []
      },
      "gridPos": { "h": 6, "w": 8, "x": 8, "y": 0 },
      "id": 2,
      "options": {
        "legend": { "calcs": ["last"], "displayMode": "list", "placement": "right" },
        "tooltip": { "mode": "single" }
      },
      "targets": [
        { "expr": "sum(rate(post_persist_total{status=\"ok\"}[5m]))", "legendFormat": "ok", "refId": "A" },
        { "expr": "sum(rate(post_persist_total{status=\"error\"}[5m]))", "legendFormat": "error", "refId": "B" }
      ],
      "title": "Скорость обработки постов",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": { "unit": "s" }, "overrides": [] },
      "gridPos": { "h": 6, "w": 8, "x": 16, "y": 0 },
      "id": 3,
      "options": {
        "legend": { "calcs": ["p50", "p90", "p99"], "displayMode": "list", "placement": "right" },
        "tooltip": { "mode": "single" }
      },
      "targets": [
        { "expr": "histogram_quantile(0.5, sum by (le) (rate(post_persist_latency_seconds_bucket[5m])))", "legendFormat": "p50", "refId": "A" },
        { "expr": "histogram_quantile(0.9, sum by (le) (rate(post_persist_latency_seconds_bucket[5m])))", "legendFormat": "p90", "refId": "B" },
        { "expr": "histogram_quantile(0.99, sum by (le) (rate(post_persist_latency_seconds_bucket[5m])))", "legendFormat": "p99", "refId": "C" }
      ],
      "title": "Латентность сохранения постов",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 6, "w": 8, "x": 0, "y": 6 },
      "id": 4,
      "options": {
        "legend": { "calcs": ["last"], "displayMode": "list", "placement": "right" },
        "tooltip": { "mode": "single" }
      },
      "targets": [
        { "expr": "sum(rate(post_persist_dlq_total[5m]))", "legendFormat": "DLQ rate", "refId": "A" }
      ],
      "title": "DLQ события",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 6, "w": 8, "x": 8, "y": 6 },
      "id": 5,
      "options": {
        "legend": { "calcs": ["last"], "displayMode": "list", "placement": "right" },
        "tooltip": { "mode": "single" }
      },
      "targets": [
        { "expr": "sum by (status) (rate(post_persist_batch_size_sum[5m]) / rate(post_persist_batch_size_count[5m]))", "legendFormat": "avg batch size", "refId": "A" }
      ],
      "title": "Размер батча (средний)",
      "type": "timeseries"
    }
  ],
  "refresh": "30s",
  "schemaVersion": 39,
  "style": "dark",
  "tags": ["telegram-assistant", "event-pipeline"],
  "templating": { "list": [] },
  "time": { "from": "now-6h", "to": "now" },
  "timezone": "browser",
  "title": "Event Pipeline Health",
  "uid": "event-pipeline-health",
  "version": 1,
  "weekStart": ""
}
