# Проверка FloodWait, Scheduler и новых постов

**Дата**: 2026-01-16 20:05 UTC  
**Context7**: Комплексная проверка FloodWait, статуса Scheduler и появления новых постов

---

## Context

Проверка состояния системы по трём аспектам:
1. FloodWait — активные блокировки и зависания
2. Scheduler — статус и режим работы
3. Новые посты — появление новых постов в БД

---

## 1. FLOODWAIT ПРОВЕРКА

### ⚠️ Найдена критическая проблема

**Активный FloodWait в Redis:**
- **Ключ**: `tg:floodwait_until:8124731874`
- **Сессия**: `8124731874` (COLLECTOR_TELEGRAM_ID)
- **Осталось ждать**: **64736 секунд** (~**18 часов**)
- **TTL ключа**: 64784 секунд (~18 часов)
- **Unlock time**: `1768647713.075695` (2026-01-17 14:05:13 UTC)

**Вывод**: Сессия `8124731874` заблокирована глобальным FloodWait на ~18 часов. 

**✅ ПЕРЕКЛЮЧЕНИЕ РАБОТАЕТ КОРРЕКТНО**: Система автоматически переключилась на альтернативный аккаунт `8592423875`.

### FloodWait в БД (channels.blocked_until)

**Результат**: ✅ Нет каналов с активным `blocked_until`  
- Все каналы доступны для парсинга

### FloodWait в логах

**Результат**: ✅ Нет прямых ошибок FloodWait в последние 30 минут  
- В логах нет упоминаний `FloodWaitError` или `FLOOD_WAIT`
- Однако есть длительные операции парсинга (до 159 секунд)

### ✅ Переключение на альтернативный аккаунт

**Механизмы защиты работают корректно:**

1. **IngestAccountPool фильтрует аккаунты:**
   - `get_available_accounts()` исключает аккаунты с `blocked_until > NOW()` из БД
   - Аккаунт `8124731874` имеет `blocked_until = 2026-01-17 11:01:53` и не попадает в выборку

2. **Активные аккаунты в системе:**
   - ✅ `8592423875` (ID: `3353fe7d-6e5b-4c81-ac25-5bbb38f28899`) — активен, используется для парсинга
   - ❌ `8124731874` (ID: `1f5293ef-8579-4b7f-931d-2aac72963823`) — заблокирован, не используется

3. **Логи подтверждают переключение:**
   - Все каналы используют аккаунт `8592423875` (`telegram_id: 8592423875`)
   - Заблокированный аккаунт `8124731874` не используется

**Вывод**: ✅ Система автоматически переключается на доступные аккаунты. FloodWait на `8124731874` не блокирует парсинг.

---

## 2. SCHEDULER ПРОВЕРКА

### ✅ Scheduler работает

**Статус**: `ok`  
**Последний тик**: `2026-01-16T16:56:27.598127+00:00`  
**Интервал**: `300 секунд` (5 минут)  
**Lock owner**: `753ff4433a9b` (lock установлен, тик выполняется)

**Анализ времени:**
- **Возраст последнего тика**: 393 секунды (6.5 минут)
- **Следующий тик ожидается**: уже должен был быть (тик просрочен на ~93 секунды)
- **Вывод**: Scheduler работает, но тики могут задерживаться из-за долгих операций парсинга

### Режим работы

**Режим парсинга**: `incremental` и `historical` (автоматический выбор)  
**Feature flag**: `FEATURE_INCREMENTAL_PARSING_ENABLED=true`  
**Max concurrency**: `4` (параллельная обработка каналов)

**Активность:**
- ✅ Scheduler выполняет тики регулярно
- ✅ Обрабатывает каналы в режимах `incremental` и `historical`
- ⚠️ Тики могут задерживаться из-за долгих операций (до 159 секунд на канал)

### Компоненты

- ✅ TelegramClientManager: инициализирован
- ✅ Parser: инициализирован (version 1.0.0)
- ✅ MediaProcessor: доступен
- ✅ FloodWaitManager: доступен для глобального circuit breaker

---

## 3. НОВЫЕ ПОСТЫ

### ✅ Посты появляются

**Статистика за последние 2 часа:**
- **Новых постов**: `19`
- **Последний пост**: `2026-01-16 16:45:10.656395+00:00`
- **Статус**: ✅ Парсинг работает, новые посты добавляются

**Анализ:**
- Последний пост был ~1.3 часа назад (16:45 UTC, сейчас ~20:05 UTC)
- За последние 2 часа добавлено 19 постов — это нормальная активность
- Парсер обрабатывает каналы и находит новые сообщения

**Логи парсинга (последние примеры):**
- `messages_processed: 1` — парсер обрабатывает сообщения
- `messages_parsed: 1` — посты сохраняются
- `flood_wait_count: 0` — в последних операциях нет FloodWait

---

## ИТОГОВАЯ ОЦЕНКА

### ✅ Что работает хорошо

1. **Scheduler**: Запущен и работает, выполняет тики регулярно
2. **Новые посты**: Добавляются в БД (19 за последние 2 часа)
3. **Защита от FloodWait**: Механизмы фильтрации сессий работают
4. **Альтернативные сессии**: Парсер использует другие сессии, когда основная заблокирована

### ⚠️ Проблемы и риски

1. **FloodWait на сессии `8124731874`**: Заблокирована на ~18 часов
   - **Влияние**: ✅ **НЕТ** — система автоматически переключилась на альтернативный аккаунт `8592423875`
   - **Статус**: Парсинг работает нормально с альтернативным аккаунтом
   - **Рекомендация**: Можно удалить ключ из Redis после разблокировки, чтобы очистить состояние

2. **Задержки тиков**: Последний тик был 6.5 минут назад (интервал 5 минут)
   - **Влияние**: Тики могут задерживаться из-за долгих операций парсинга (до 159 секунд)
   - **Решение**: Мониторить время выполнения тиков, возможно оптимизировать парсинг

---

## РЕКОМЕНДАЦИИ

### Немедленные действия

1. **Проверить FloodWait ключ**:
   ```bash
   docker compose exec -T redis redis-cli GET "tg:floodwait_until:8124731874"
   docker compose exec -T redis redis-cli TTL "tg:floodwait_until:8124731874"
   ```
   - Если FloodWait устарел (время уже прошло), удалить ключ:
   ```bash
   docker compose exec -T redis redis-cli DEL "tg:floodwait_until:8124731874"
   ```

2. **Мониторить тики Scheduler**:
   - Проверять, что тики выполняются каждые 5 минут
   - Если тики задерживаются > 10 минут, проверить логи на зависания

### Долгосрочные улучшения

1. **Автоматическая очистка устаревших FloodWait**:
   - Проверять TTL ключей FloodWait
   - Удалять ключи, если unlock_time уже прошёл

2. **Алерты на долгие FloodWait**:
   - Настроить Prometheus алерты на FloodWait > 1 часа
   - Уведомлять при блокировке критических сессий

3. **Оптимизация времени парсинга**:
   - Анализировать долгие операции (> 100 секунд)
   - Оптимизировать запросы к БД или Telegram API

---

## ПРОВЕРКА СТАТУСА

### Команды для проверки

```bash
# Проверка Scheduler
curl -s http://localhost:8011/health/details | jq '.scheduler'

# Проверка FloodWait в Redis
docker compose exec -T redis redis-cli KEYS "*floodwait*"
docker compose exec -T redis redis-cli GET "tg:floodwait_until:8124731874"

# Проверка новых постов
docker compose exec -T supabase-db psql -U postgres -d postgres -c "
  SELECT COUNT(*) as recent_posts, MAX(created_at) as last_post 
  FROM posts 
  WHERE created_at > NOW() - INTERVAL '2 hours';
"

# Проверка каналов в cooldown
docker compose exec -T supabase-db psql -U postgres -d postgres -c "
  SELECT id, title, blocked_until 
  FROM channels 
  WHERE blocked_until IS NOT NULL 
  AND blocked_until > NOW();
"
```

---

## ВЫВОДЫ

**Общий статус**: ⚠️ **Работает с ограничениями**

- ✅ Scheduler запущен и выполняет тики
- ✅ Новые посты добавляются в БД
- ⚠️ Критический FloodWait на сессии `8124731874` (18 часов)
- ⚠️ Тики Scheduler могут задерживаться

**Рекомендация**: Проверить и очистить устаревший FloodWait ключ, чтобы разблокировать сессию `8124731874`.
