# SearXNG Credentials Setup

**Дата**: 2025-02-02  
**Статус**: ✅ Инструкции по настройке учетных данных

## ❓ Где взять SEARXNG_USER и SEARXNG_PASSWORD?

### Вариант 1: Локальный SearXNG контейнер (рекомендуется)

**Для локального контейнера** (`http://searxng:8080`):
- ✅ **НЕ требуются** учетные данные
- ✅ Оставьте пустыми в `.env`:
  ```bash
  SEARXNG_URL=http://searxng:8080
  SEARXNG_USER=
  SEARXNG_PASSWORD=
  ```

### Вариант 2: Внешний SearXNG инстанс (https://searxng.produman.studio)

**Context7 Best Practice**: BasicAuth для SearXNG обычно настраивается на уровне reverse proxy (Caddy/Nginx), а не самого SearXNG.

#### Проверка требований аутентификации

```bash
# Проверка без аутентификации
curl -v "https://searxng.produman.studio/search?q=test&format=json" 2>&1 | grep -iE "HTTP|401|403|WWW-Authenticate"

# Если возвращает 401/403 с WWW-Authenticate: Basic - требуется BasicAuth
# Если возвращает 200 - аутентификация не требуется
```

#### Получение учетных данных

**Если инстанс требует BasicAuth:**

1. **Обратитесь к администратору** `https://searxng.produman.studio`
   - Запросите учетные данные (username/password)
   - Или создайте учетную запись на инстансе

2. **Проверьте Caddyfile** (если инстанс на вашем сервере):
   ```bash
   grep -A 5 "searxng.produman.studio" Caddyfile
   ```
   
   Если есть блок `basicauth`, учетные данные могут быть там или в переменных окружения.

3. **Проверьте переменные окружения**:
   ```bash
   docker compose exec caddy env | grep -i searxng
   ```

#### Если инстанс НЕ требует BasicAuth

**Оставьте переменные пустыми**:
```bash
SEARXNG_URL=https://searxng.produman.studio
SEARXNG_USER=
SEARXNG_PASSWORD=
```

## 📝 Текущая ситуация

**В проекте используется локальный SearXNG контейнер:**
- ✅ URL: `http://searxng:8080` (внутри Docker network)
- ✅ BasicAuth: НЕ требуется
- ✅ Network: Только внутри Docker network

**Внешний инстанс `https://searxng.produman.studio`:**
- ⚠️ Требует проверки доступности и требований к аутентификации
- ⚠️ Если требует BasicAuth - нужны учетные данные от администратора

## ✅ Рекомендация (Context7 Best Practice)

**Используйте локальный SearXNG контейнер:**
1. ✅ Не требует учетных данных
2. ✅ Работает внутри Docker network
3. ✅ Полный контроль над конфигурацией
4. ✅ Нет зависимости от внешнего сервиса

**Для переключения на локальный контейнер:**
```bash
# В .env
SEARXNG_URL=http://searxng:8080
SEARXNG_USER=
SEARXNG_PASSWORD=

# Запуск SearXNG
docker compose --profile rag up -d searxng
```

## 🔍 Проверка текущей конфигурации

```bash
# Проверка настроек
docker compose exec api python3 -c "
from config import settings
print(f'URL: {settings.searxng_url}')
print(f'User: {settings.searxng_user or \"(не задан)\"}')
print(f'Password: {\"***\" if settings.searxng_password else \"(не задан)\"}')
"
```

