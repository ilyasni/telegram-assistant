# Быстрый старт: Агенты трендов

**Дата**: 2025-11-16

## ✅ Система работает автоматически!

Все агенты запускаются автоматически при старте контейнеров:

### Что уже работает:

1. **Trend Editor Agent** — запускается автоматически в worker
   - Подписывается на `trends.emerging`
   - Обрабатывает карточки в реальном времени
   - ✅ Переменные окружения установлены в `docker-compose.yml`

2. **Trend QA/Filter Agent** — работает автоматически в API
   - Вызывается при каждом запросе к `/api/trends/*`
   - Фильтрует тренды перед показом
   - ✅ Переменные окружения установлены в `docker-compose.yml`

3. **User Interest Personalizer Agent** — запускается по расписанию
   - Ежедневно в 02:00 UTC
   - Обновляет профили пользователей
   - ✅ Зарегистрирован в scheduler

4. **Threshold Tuner Agent** — запускается по расписанию
   - Еженедельно в воскресенье 03:00 UTC
   - Анализирует пороги
   - ✅ Зарегистрирован в scheduler

5. **Taxonomy Agent** — вызывается автоматически
   - Интегрирован в Editor Agent
   - Категоризирует тренды
   - ✅ Работает автоматически

---

## 🔍 Проверка работы

### 1. Проверить переменные окружения

```bash
# Worker
docker compose exec worker env | grep TREND_EDITOR

# API
docker compose exec api env | grep TREND_QA
```

### 2. Проверить логи

```bash
# Trend Editor Agent
docker compose logs worker | grep -i "trend_editor"

# Scheduler (Personalizer, Tuner)
docker compose logs api | grep -i "scheduler\|update_user_trend_profiles\|analyze_trend_thresholds"
```

### 3. Проверить метрики

```bash
# Prometheus
curl http://localhost:9090/api/v1/query?query=trend_editor_requests_total

# Grafana
# Откройте: https://grafana.produman.studio/d/trend-agents-monitoring
```

### 4. Использовать скрипт проверки

```bash
./scripts/check_trend_agents.sh
```

---

## ⚠️ Важно

**Ничего дополнительно запускать не нужно!**

Система работает автоматически:
- ✅ Контейнеры запущены
- ✅ Переменные окружения установлены
- ✅ Миграция БД применена
- ✅ Агенты зарегистрированы

**Агенты начнут работать когда:**
- **Editor Agent** — появятся события в `trends.emerging`
- **QA Agent** — будут запросы к `/api/trends/*`
- **Personalizer Agent** — наступит 02:00 UTC
- **Tuner Agent** — наступит воскресенье 03:00 UTC

---

## 📊 Мониторинг

- **Grafana**: https://grafana.produman.studio/d/trend-agents-monitoring
- **Prometheus**: http://localhost:9090
- **Логи**: `docker compose logs worker api`

---

## 🎉 Готово!

Система полностью автоматизирована и работает без дополнительных действий.

