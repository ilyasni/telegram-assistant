# Почему нет устойчивых трендов сразу после обнаружения?

## Context

При обнаружении трендов через кнопку "🔍 Обнаружить" создаются только **emerging** (горячие) тренды. **Stable** (устойчивые) тренды появляются позже автоматически.

## Как работает система

### 1. Обнаружение трендов (`detect_trends_task`)

Когда вы нажимаете "🔍 Обнаружить":
- Анализируются посты за последние 7 дней
- Создаются **emerging** тренды (статус `emerging`)
- Эти тренды имеют низкие метрики (2 упоминания, 1 источник)

### 2. Стабилизация трендов (`trends_stable_task`)

Каждый час автоматически запускается задача, которая:
- Проверяет все **emerging** тренды
- Переводит их в **stable**, если они проходят пороги:
  - `freq_long >= 30` (минимум 30 упоминаний за 24 часа)
  - `source_diversity >= 3` (минимум 3 разных источника)
  - `burst_score >= 1.5` (минимальный всплеск активности)

### 3. Почему нет stable трендов сразу?

**Это нормально!** Причина:

1. **Низкие метрики**: Только что обнаруженные тренды имеют:
   - `window_mentions = 2` (нужно >= 30)
   - `sources_count = 1` (нужно >= 3)
   - `burst_score = 20` ✅ (проходит порог >= 1.5)

2. **Время накопления**: Тренды должны набрать метрики со временем:
   - Больше постов из разных каналов
   - Больше упоминаний за 24 часа
   - Больше источников

3. **Автоматическая стабилизация**: Через несколько часов/дней, когда тренды наберут достаточно метрик, они автоматически станут **stable**.

## Что делать?

### Вариант 1: Подождать (рекомендуется)

- Подождите несколько часов/дней
- Тренды автоматически станут **stable**, когда наберут метрики
- Задача `trends_stable_task` запускается каждый час

### Вариант 2: Уменьшить пороги

Если нужно видеть stable тренды сразу, можно уменьшить пороги в `api/tasks/scheduler_tasks.py`:

```python
min_freq = getattr(settings, "trend_stable_min_freq", 30)  # Уменьшить до 5-10
min_sources = getattr(settings, "trend_stable_min_sources", 3)  # Уменьшить до 1-2
min_burst = getattr(settings, "trend_stable_min_burst", 1.5)  # Оставить 1.5
```

**⚠️ Внимание**: Слишком низкие пороги могут привести к появлению шумных трендов.

### Вариант 3: Подключить больше каналов

- Больше каналов = больше постов = больше метрик
- Тренды быстрее наберут нужные метрики

## Текущее состояние

- **Emerging тренды**: 1137 (созданы при обнаружении)
- **Stable тренды**: 0 (не набрали метрики)
- **Кластеры с высокими метриками**: 1 (почти готов к стабилизации)

## Вывод

**Да, так и должно быть!** При обнаружении трендов создаются только emerging тренды. Stable тренды появляются автоматически через несколько часов/дней, когда они наберут достаточно метрик (30+ упоминаний, 3+ источника).

Это нормальное поведение системы - она фильтрует тренды по качеству, чтобы показывать только действительно устойчивые тренды.

