# SearXNG - Исправление проблемы с правами доступа

**Дата**: 2025-11-05  
**Статус**: ✅ Решение готово

## ❌ Проблема

```
Failed to save 'settings.yml': Unable to write file
Error: EACCES: permission denied
```

**Причина:** Файлы в каталоге `searxng/` принадлежат пользователю `977:977` (searxng), а текущий пользователь `ilyasni` не может их редактировать.

## ✅ Решение

### Вариант 1: Запустить скрипт с sudo (рекомендуется)

```bash
sudo ./scripts/fix_searxng_permissions.sh
```

### Вариант 2: Выполнить команды вручную

```bash
sudo chown -R $USER:$USER /opt/telegram-assistant/searxng
sudo chmod -R 755 /opt/telegram-assistant/searxng
```

### Вариант 3: Использовать обновленный скрипт (с интерактивным sudo)

```bash
./scripts/setup_searxng_permissions.sh
```

Скрипт запросит пароль sudo интерактивно.

## 📋 Проверка

После исправления прав:

```bash
ls -la searxng/
```

Должно быть:
```
drwxr-xr-x 2 ilyasni ilyasni 4096 Nov  5 ...
-rw-r--r-- 1 ilyasni ilyasni 1956 Nov  5 settings.yml
```

## 💡 Почему это происходит

1. SearXNG контейнер работает под uid/gid `977:977`
2. При создании файлов контейнером они получают владельца `977:977`
3. Пользователь `ilyasni` (uid `1000`) не может редактировать файлы без прав

## 🔄 Постоянное решение

Для автоматической настройки прав при каждом запуске можно добавить в `docker-compose.yml`:

```yaml
command: >
  sh -c "
    chown -R 977:977 /etc/searxng &&
    exec /sbin/tini -- /usr/local/searxng/dockerfiles/docker-entrypoint.sh
  "
```

Но это не решит проблему редактирования файлов с хоста.

## 📝 Context7 Best Practices

1. ✅ Использовать ACL для предоставления прав нескольким пользователям
2. ✅ Настраивать права один раз через скрипт
3. ✅ Использовать группу для совместного доступа

## 🔗 Ссылки

- [Docker Volume Permissions](https://docs.docker.com/storage/volumes/)
- [Linux File Permissions](https://www.linux.com/training-tutorials/understanding-linux-file-permissions/)

