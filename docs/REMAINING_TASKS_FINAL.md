# Оставшиеся задачи по доработкам

**Дата**: 2025-02-02  
**Статус**: Критичные и опциональные доработки

## ✅ Завершенные основные функции

1. ✅ **pgvector extension** - установлен и настроен
2. ✅ **Векторный поиск трендов** - реализован (поиск похожих, дедупликация, группировка, кластеризация)
3. ✅ **RAG Service** - интеллектуальный поиск и ответы на вопросы
4. ✅ **Digest Service** - генерация дайджестов по темам
5. ✅ **Trend Detection** - обнаружение трендов через multi-agent систему
6. ✅ **Voice Transcription** - транскрибация через SaluteSpeech
7. ✅ **Neo4j интеграция** - для RAG, Digest, Trends, Recommendations
8. ✅ **User Interests Tracking** - отслеживание интересов пользователей
9. ✅ **Scheduled Tasks** - APScheduler для дайджестов и трендов
10. ✅ **API endpoints** - все CRUD операции для дайджестов и трендов

---

## 🔴 Критичные задачи (высокий приоритет)

### 1. Отправка дайджестов пользователю через бота

**Файл**: `api/tasks/scheduler_tasks.py` (строка 69)

**Статус**: TODO - дайджест генерируется, но не отправляется пользователю

**Требуется**:
- Получить `telegram_id` пользователя из таблицы `users`
- Отправить дайджест через Telegram Bot API
- Обновить статус в `digest_history` с `pending` на `sent`
- Обработать ошибки отправки (обновить статус на `failed`)

**Пример реализации**:
```python
# В generate_digest_for_user после сохранения в БД
from aiogram import Bot
from config import settings

bot = Bot(token=settings.telegram_bot_token)
user = db.query(User).filter(User.id == UUID(user_id)).first()

if user and user.telegram_id:
    try:
        await bot.send_message(
            chat_id=user.telegram_id,
            text=f"📰 Ваш дайджест за {digest_history.digest_date}\n\n{digest_content.content}",
            parse_mode="HTML"
        )
        digest_history.status = "sent"
        digest_history.sent_at = datetime.now(timezone.utc)
        db.commit()
    except Exception as e:
        logger.error("Error sending digest", user_id=user_id, error=str(e))
        digest_history.status = "failed"
        db.commit()
```

**Приоритет**: 🔴 ВЫСОКИЙ (основная функция дайджестов)

---

### 2. Сохранение RAG запросов в историю

**Файл**: `api/services/rag_service.py` (строка 767)

**Статус**: TODO - запросы обрабатываются, но не сохраняются

**Требуется**:
- Сохранять каждый RAG запрос в `rag_query_history`
- Сохранять: `query_text`, `intent`, `confidence`, `response_text`, `sources_count`, `processing_time_ms`
- Для голосовых запросов: `audio_file_id`, `transcription_text`, `transcription_provider`

**Пример реализации**:
```python
# После генерации ответа в RAGService.query()
from models.database import RAGQueryHistory
from datetime import datetime, timezone

rag_history = RAGQueryHistory(
    user_id=user_id,
    query_text=query,
    query_type=intent,
    intent=intent,
    confidence=confidence,
    response_text=answer,
    sources_count=len(sources),
    processing_time_ms=int((time.time() - start_time) * 1000),
    audio_file_id=audio_file_id if voice_transcription else None,
    transcription_text=transcription if voice_transcription else None,
    transcription_provider="salutespeech" if voice_transcription else None
)
db.add(rag_history)
db.commit()
```

**Приоритет**: 🔴 ВЫСОКИЙ (важно для аналитики и улучшения качества)

---

## 🟡 Средние задачи (средний приоритет)

### 3. Проверка статуса индексации перед RAG запросом

**Файл**: `api/routers/rag.py` (строка 102)

**Статус**: TODO - проверка не реализована

**Требуется**:
- Проверять наличие индексированных постов в Qdrant для tenant_id
- Если постов нет - возвращать информативное сообщение
- Можно добавить проверку через Qdrant API или через счетчик в Redis

**Приоритет**: 🟡 СРЕДНИЙ (улучшает UX)

---

### 4. DLQ для failed синхронизации интересов

**Файл**: `api/tasks/scheduler_tasks.py` (строка 251)

**Статус**: TODO - ошибки логируются, но не попадают в DLQ

**Требуется**:
- Добавить отправку в Redis Stream `user_interests.sync.failed`
- Включить: `user_id`, `topic`, `weight`, `error`, `timestamp`
- Можно добавить retry механизм

**Приоритет**: 🟡 СРЕДНИЙ (улучшает надежность)

---

### 5. Отправка уведомлений о трендах пользователям

**Статус**: Не реализовано

**Требуется**:
- После обнаружения тренда проверять, какие пользователи подписаны на связанные каналы/темы
- Отправлять уведомления через Telegram Bot
- Сохранять в `trend_alerts`

**Приоритет**: 🟡 СРЕДНИЙ (важная функция, но не критична для запуска)

---

## 🟢 Низкие задачи (опциональные улучшения)

### 6. Валидация инвайт-кода

**Файл**: `api/routers/users.py` (строка 102)

**Статус**: TODO - валидация пропущена

**Требуется**:
- Проверять существование инвайт-кода в БД
- Проверять лимиты использования
- Проверять срок действия

**Приоритет**: 🟢 НИЗКИЙ (можно отложить)

---

### 7. Полноценная JWT генерация/верификация

**Файл**: `api/routers/tg_webapp_auth.py` (строки 46, 210)

**Статус**: TODO - упрощенная реализация

**Требуется**:
- Использовать правильный JWT secret из настроек
- Подписывать JWT с правильным алгоритмом
- Верифицировать подпись при получении

**Приоритет**: 🟢 НИЗКИЙ (текущая реализация работает для MVP)

---

### 8. Получение реального ID канала

**Файл**: `api/bot/handlers.py` (строка 471)

**Статус**: TODO - используется заглушка

**Требуется**:
- Получать реальный `telegram_id` канала из БД
- Использовать из таблицы `channels` или из контекста запроса

**Приоритет**: 🟢 НИЗКИЙ (текущая заглушка не критична)

---

### 9. Вызов telethon-ingest API

**Файл**: `api/routers/channels.py` (строка 625)

**Статус**: TODO - прямой вызов не реализован

**Требуется**:
- Реализовать HTTP клиент для вызова telethon-ingest API
- Или использовать Redis Streams для отправки событий парсинга

**Приоритет**: 🟢 НИЗКИЙ (можно использовать существующий механизм)

---

### 10. Индекс для post_enrichment.kind

**Файл**: `docs/REMAINING_TASKS.md`

**Статус**: Требует проверки

**Требуется**:
- Проверить наличие индекса: `SELECT indexname FROM pg_indexes WHERE tablename = 'post_enrichment' AND indexname LIKE '%kind%';`
- Если нет - добавить: `CREATE INDEX IF NOT EXISTS idx_post_enrichment_kind ON post_enrichment(kind) WHERE kind IS NOT NULL;`

**Приоритет**: 🟢 НИЗКИЙ (влияет на производительность, но не критично)

---

### 11. Миграция legacy полей в post_enrichment

**Файл**: `docs/REMAINING_TASKS.md`

**Статус**: Опционально

**Требуется**:
- Мигрировать `ocr_text`, `vision_labels`, `crawl_md` в `data` JSONB
- Можно отложить до полного перехода на новую схему

**Приоритет**: 🟢 НИЗКИЙ (можно отложить)

---

## 📊 Итоговый статус

### ✅ Готово к использованию
- Все основные функции реализованы
- pgvector установлен и работает
- Векторный поиск трендов функционирует
- API endpoints готовы

### 🔴 Критичные доработки (2 задачи)
1. Отправка дайджестов через бота
2. Сохранение RAG запросов в историю

### 🟡 Средние доработки (3 задачи)
3. Проверка статуса индексации
4. DLQ для синхронизации интересов
5. Уведомления о трендах

### 🟢 Опциональные улучшения (6 задач)
6-11. Различные улучшения и оптимизации

---

## Рекомендации по приоритетам

**Для запуска в production**:
1. ✅ Реализовать отправку дайджестов (критично)
2. ✅ Сохранение RAG истории (критично для аналитики)
3. ⚠️ Проверка индексации (улучшает UX)

**После запуска**:
- DLQ для интересов
- Уведомления о трендах
- Остальные опциональные улучшения

---

## Вывод

**Основной функционал готов.** Осталось реализовать 2 критичные задачи для полноценной работы системы:
- Отправка дайджестов пользователям
- Сохранение истории RAG запросов

Остальные задачи можно выполнять по мере необходимости.

