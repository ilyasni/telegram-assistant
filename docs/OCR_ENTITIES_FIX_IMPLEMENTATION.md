# Исправление промпта для извлечения OCR Entities

**Дата**: 2025-12-05  
**Context7**: Улучшение промпта на основе анализа реальных данных и best practices

---

## Context

Анализ реальной записи из БД показал проблему: промпт для entity extraction слишком узкий и не извлекает entities из политических текстов.

**Реальная запись**: Post ID `696157be-f911-496b-9944-22ae82e79a83`
- OCR текст: про бельгийского политика де Бевера
- `entities: []` - пустой массив
- Ожидаемые entities: "Европейская комиссия" (ORG), "Киев" (LOC), "де Бевер" (PERSON), "Бельгия" (LOC)

---

## Проблема

### Текущий промпт

```python
"""Извлеки сущности из OCR текста. Типы:
- ORG (обязательно): банки, магазины, сервисы
- PRODUCT (обязательно): тип карт, эквайринг, конкретные продукты
- PERSON (опционально): если явно присутствует (имена, должности)
- LOC (опционально): города, адреса
"""
```

**Проблемы**:
1. ❌ Слишком узкая специализация (только банки/карты)
2. ❌ Требует обязательные ORG и PRODUCT
3. ❌ Не покрывает политический контекст
4. ❌ Нет примеров извлечения

---

## Решение

### Улучшенный универсальный промпт

**Файл**: `api/worker/services/ocr_enhancement_service.py` (строки 183-195)

**Изменения**:
1. ✅ Расширенные типы сущностей с примерами
2. ✅ Поддержка разных контекстов (политика, финансы, новости)
3. ✅ Убраны обязательные требования
4. ✅ Добавлены примеры извлечения
5. ✅ Учет опечаток OCR

**Новый промпт**:
```python
"""Ты — эксперт по извлечению именованных сущностей (NER) из текста.

Извлеки все именованные сущности из OCR текста. Типы сущностей:
- ORG: организации, компании, банки, правительственные органы, международные организации, министерства, комиссии
  Примеры: "Сбербанк", "Европейская комиссия", "ЕС", "Еврактив", "ВТБ", "правительство", "министерство"
- PERSON: имена людей, фамилии, политические деятели (включая должности с именами)
  Примеры: "де Бевер", "Петр Иванов", "президент", "министр", "Крис"
- LOC: города, страны, регионы, географические названия
  Примеры: "Киев", "Бельгия", "Россия", "Москва", "Европа", "Украина"
- PRODUCT: конкретные продукты, услуги, финансовые инструменты (только если это конкретное название)
  Примеры: "дебетовая карта Visa", "эквайринг", "iPhone 15", "кредит"

ВАЖНО:
- Извлекай сущности даже из текста с опечатками OCR (например, "Bевер" → "Бевер", "Киeвy" → "Киев")
- Используй контекст для определения типа (например, "Европейская комиссия" = ORG, "премьер-министр Бельгии" = PERSON)
- Если сущность упоминается несколько раз, используй наиболее полную и точную форму
- Извлекай только явные именованные сущности, не общие слова или абстрактные понятия
- Если сущностей нет, верни пустой массив []

Верни ТОЛЬКО валидный JSON массив без дополнительного текста, комментариев и markdown блоков:
[{"text": "название сущности", "type": "ORG|PERSON|LOC|PRODUCT", "confidence": 0.0-1.0}]"""
```

---

## Дополнительные улучшения

### 1. Детальное логирование

Добавлено логирование в `extract_entities()`:
- ✅ Ответ LLM перед парсингом (первые 300 символов)
- ✅ Количество найденных и валидных entities
- ✅ Причины фильтрации entities
- ✅ Детальные ошибки парсинга JSON

### 2. Улучшенная обработка ошибок

- ✅ Логирование preview ответа при ошибке парсинга
- ✅ Логирование типа ошибки
- ✅ Traceback для критических ошибок

---

## Ожидаемые результаты

### Для примера текста

**Текст**: "...Европейской комйссий... замороженные российские аKTИBы... кредита Киeвy... де Bевер..."

**Ожидаемые entities**:
```json
[
  {"text": "Европейская комиссия", "type": "ORG", "confidence": 0.9},
  {"text": "Киев", "type": "LOC", "confidence": 0.95},
  {"text": "Россия", "type": "LOC", "confidence": 0.9},
  {"text": "де Бевер", "type": "PERSON", "confidence": 0.85},
  {"text": "Бельгия", "type": "LOC", "confidence": 0.9}
]
```

---

## Checks

### Проверка изменений

```bash
# Проверить новый промпт в коде
grep -A 20 "entity_extraction_prompt" api/worker/services/ocr_enhancement_service.py

# Проверить логирование
grep -A 5 "LLM response received for entity extraction" api/worker/services/ocr_enhancement_service.py
```

### Тестирование

```bash
# Тест на реальном тексте
python3 scripts/test_extract_entities_real.py

# Проверка логов после изменений
docker logs telegram-assistant-worker-1 2>&1 | grep -i "entity extraction\|extract_entities" | tail -20
```

---

## Impact / Rollback

### Impact

**Положительные изменения**:
- ✅ Улучшит извлечение entities из разных типов текстов
- ✅ Покроет политический, финансовый, новостной контекст
- ✅ Увеличит количество записанных entities в Neo4j
- ✅ Детальное логирование поможет в диагностике

**Ожидаемый результат**:
- Entities будут извлекаться из политических текстов
- Больше entities будет записываться в Neo4j
- Логи покажут причины пустых результатов

### Rollback

**Простой откат**:
1. Вернуть старый промпт в `ocr_enhancement_service.py`
2. Удалить дополнительное логирование (опционально)

---

## Следующие шаги

1. ✅ **Промпт улучшен** - применен универсальный промпт
2. ✅ **Логирование добавлено** - детальные логи для диагностики
3. ⏭️ **Тестирование** - запустить тест на реальных данных
4. ⏭️ **Мониторинг** - проверить результаты после перезапуска worker

---

**Исправление применено согласно Context7 best practices!**

