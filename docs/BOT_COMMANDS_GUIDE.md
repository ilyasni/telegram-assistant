# Руководство по командам Telegram бота

**Дата**: 2025-11-05  
**Context7**: Полное руководство по управлению дайджестами, трендами и остальным функционалом через Telegram бота

---

## Основные команды

### `/start`
Регистрация и начало работы с ботом.

### `/digest`
Управление дайджестами новостей.

**Доступные действия:**
- ⚙️ **Настройки** — настройка тем, времени отправки, частоты
- 📰 **История** — просмотр истории дайджестов
- 🔄 **Сгенерировать сейчас** — немедленная генерация дайджеста

### `/trends`
Управление трендами в каналах.

**Доступные действия:**
- 📈 **Список трендов** — просмотр активных трендов
- 🔍 **Обнаружить тренды** — запуск обнаружения трендов

### `/search <запрос>`
Гибридный поиск по постам и веб-контенту.

### `/recommend <запрос>`
Рекомендации на основе истории запросов.

### `/my_channels`
Список подписанных каналов.

### `/add_channel`
Добавление нового канала.

### `/subscription`
Информация о подписке и тарифе.

---

## Управление дайджестами

### Настройка дайджеста

1. **Отправьте команду**: `/digest`
2. **Выберите**: ⚙️ Настройки
3. **Настройте параметры**:
   - **Включение/выключение**: кнопка переключения статуса
   - **Темы**: нажмите "📝 Темы" и отправьте список тем через запятую
     - Пример: `AI, машинное обучение, нейросети`
   - **Время отправки**: нажмите "⏰ Время отправки" и отправьте время в формате HH:MM
     - Пример: `09:00` или `18:30`
   - **Частота**: нажмите "📅 Частота" и выберите из списка
     - 📅 Ежедневно
     - 📆 Еженедельно
     - 📊 Ежемесячно

### Генерация дайджеста

1. **Отправьте команду**: `/digest`
2. **Выберите**: 🔄 Сгенерировать сейчас
3. **Дождитесь результата** (может занять до 60 секунд)

**Примечание**: Для генерации дайджеста необходимо указать хотя бы одну тему в настройках.

### Просмотр истории

1. **Отправьте команду**: `/digest`
2. **Выберите**: 📰 История
3. Просмотрите последние 5 дайджестов

---

## Управление трендами

### Просмотр трендов

1. **Отправьте команду**: `/trends`
2. **Выберите**: 📈 Список трендов
3. Просмотрите список активных трендов с:
   - Ключевым словом
   - Частотой упоминаний
   - Ростом популярности
   - Engagement score

### Детали тренда

1. Выберите тренд из списка
2. Просмотрите:
   - Статистику (частота, рост, engagement)
   - Даты первого и последнего упоминания
   - Затронутые каналы
   - Примеры постов
3. **Доступные действия**:
   - 📋 Похожие тренды — найти похожие тренды
   - 🗄 Архивировать — архивировать тренд

### Обнаружение трендов

1. **Отправьте команду**: `/trends`
2. **Выберите**: 🔍 Обнаружить тренды
3. Дождитесь завершения анализа (может занять до 2 минут)

**Параметры обнаружения** (по умолчанию):
- Период анализа: 7 дней
- Минимальная частота: 10 упоминаний
- Минимальный рост: 20%
- Минимальный engagement: 5.0

---

## Интерактивное меню

### Главное меню

Доступно через команду `/start` или кнопку "🔙 Главное меню":

- 📊 Мои каналы
- ➕ Добавить канал
- 🔍 Поиск
- 📰 Дайджесты
- 📈 Тренды
- 💎 Подписка

---

## Примеры использования

### Пример 1: Настройка дайджеста

```
Пользователь: /digest
Бот: [Меню дайджестов]

Пользователь: [Нажимает ⚙️ Настройки]
Бот: [Показывает текущие настройки]

Пользователь: [Нажимает 📝 Темы]
Бот: "Укажите темы через запятую..."

Пользователь: AI, машинное обучение, нейросети
Бот: "✅ Темы обновлены! Добавлено тем: 3"

Пользователь: [Нажимает 🔴 ✅ Включен]
Бот: "✅ Дайджест включен"
```

### Пример 2: Генерация дайджеста

```
Пользователь: /digest
Бот: [Меню дайджестов]

Пользователь: [Нажимает 🔄 Сгенерировать сейчас]
Бот: "⏳ Генерирую дайджест..."

[Через 30-60 секунд]
Бот: "📰 Дайджест готов!
      📊 Постов: 15
      📝 Темы: AI, машинное обучение
      
      ## Тема 1: Искусственный интеллект
      - Пункт 1: ...
      - Пункт 2: ..."
```

### Пример 3: Обнаружение трендов

```
Пользователь: /trends
Бот: [Меню трендов]

Пользователь: [Нажимает 🔍 Обнаружить тренды]
Бот: "⏳ Обнаружаю тренды..."

[Через 60-120 секунд]
Бот: "✅ Обнаружение трендов завершено!
      📊 Найдено трендов: 5
      
      Топ тренды:
      1. ChatGPT (45 упоминаний)
      2. GPT-4 (32 упоминания)
      ..."
```

---

## Обработка ошибок

### Ошибка: "Пользователь не найден"

**Причина**: Пользователь не зарегистрирован в системе.

**Решение**: Используйте `/start` для регистрации.

---

### Ошибка: "topics обязателен когда enabled=True"

**Причина**: Попытка включить дайджест без указания тем.

**Решение**: 
1. Откройте настройки дайджеста
2. Нажмите "📝 Темы"
3. Укажите хотя бы одну тему

---

### Ошибка: "Неверный формат времени"

**Причина**: Время указано в неправильном формате.

**Решение**: Используйте формат HH:MM (например: `09:00`, `18:30`).

---

### Ошибка: "Превышено время ожидания"

**Причина**: Операция занимает больше времени, чем ожидается.

**Решение**: 
- Для генерации дайджеста: проверьте настройки и попробуйте позже
- Для обнаружения трендов: это нормально, результаты будут доступны позже

---

## Context7 Best Practices

### Валидация данных

- **Темы**: Минимум 1 тема обязательна для включенного дайджеста
- **Время**: Формат HH:MM (00:00 - 23:59)
- **Частота**: daily, weekly, monthly

### Идемпотентность

- Все операции с настройками идемпотентны
- Повторная генерация дайджеста создает новую запись в истории
- Обнаружение трендов можно запускать многократно

### Обработка ошибок

- Все ошибки логируются с контекстом
- Пользователю показываются понятные сообщения об ошибках
- Критичные ошибки не прерывают работу бота

---

## Технические детали

### API Endpoints

**Дайджесты:**
- `GET /api/digest/settings/{user_id}` — получение настроек
- `PUT /api/digest/settings/{user_id}` — обновление настроек
- `GET /api/digest/history/{user_id}` — история дайджестов
- `POST /api/digest/generate/{user_id}` — генерация дайджеста

**Тренды:**
- `GET /api/trends/` — список трендов
- `GET /api/trends/{trend_id}` — детали тренда
- `POST /api/trends/detect` — обнаружение трендов
- `POST /api/trends/{trend_id}/archive` — архивирование тренда
- `GET /api/trends/{trend_id}/similar` — похожие тренды

### FSM States

**DigestStates:**
- `waiting_topics` — ожидание ввода тем
- `waiting_schedule_time` — ожидание времени отправки
- `waiting_frequency` — ожидание частоты (через callback)

### Callback Data

**Дайджесты:**
- `digest:menu` — главное меню
- `digest:settings` — настройки
- `digest:toggle` — переключение включения/выключения
- `digest:edit_topics` — редактирование тем
- `digest:edit_time` — редактирование времени
- `digest:edit_frequency` — редактирование частоты
- `digest:frequency:{daily|weekly|monthly}` — выбор частоты
- `digest:history` — история
- `digest:generate` — генерация

**Тренды:**
- `trends:menu` — главное меню
- `trends:list` — список трендов
- `trends:detect` — обнаружение трендов
- `trend:view:{trend_id}` — детали тренда
- `trend:archive:{trend_id}` — архивирование
- `trend:similar:{trend_id}` — похожие тренды

---

## Следующие шаги

1. ✅ Команды для дайджестов реализованы
2. ✅ Команды для трендов реализованы
3. ⏳ Протестировать на реальных данных
4. ⏳ Добавить команду `/help` с описанием всех команд
5. ⏳ Добавить команду `/interests` для просмотра интересов пользователя

