# TODO: Улучшения системы обнаружения трендов

## Context

После исправления критических проблем остались задачи для улучшения системы согласно best practices (Context7).

## ✅ Выполнено

1. **Исправлены дубликаты кластеров**
   - Добавлена проверка `_find_cluster_by_label` перед созданием нового кластера
   - Объединено 280 дубликатов через скрипт

2. **Исправлено сохранение постов**
   - Добавлен fallback на `channel_title` при пустом snippet
   - Посты сохраняются даже без текста

3. **Исправлен расчет burst_score**
   - Минимальный baseline = 0.1 вместо 1.0
   - Пересчитано 1137 кластеров

4. **Исправлены дублирующиеся кнопки**
   - Кнопки навигации вынесены из цикла

## 🔄 В процессе / Требует проверки

### 1. Проверка работы trends_stable_task

**Проблема**: Есть кластеры с высокими метриками, но они не стали stable.

**Проверка**:
```bash
# Проверить, запускается ли задача
docker logs api | grep "trends_stable_task"

# Проверить кластеры, готовые к стабилизации
psql -c "SELECT COUNT(*) FROM trend_clusters c JOIN trend_metrics m ON c.id = m.cluster_id WHERE c.status = 'emerging' AND m.freq_long >= 30 AND m.source_diversity >= 3 AND m.burst_score >= 1.5;"
```

**Действие**: Убедиться, что задача запускается каждый час и обрабатывает кластеры.

### 2. Обработка новых постов

**Проблема**: Новые посты не обрабатываются (0 постов за последние 24 часа).

**Проверка**:
```bash
# Проверить, есть ли новые посты
psql -c "SELECT COUNT(*) FROM posts WHERE created_at > NOW() - INTERVAL '24 hours';"

# Проверить, обрабатываются ли они
psql -c "SELECT COUNT(*) FROM trend_cluster_posts WHERE created_at > NOW() - INTERVAL '24 hours';"
```

**Действие**: Убедиться, что worker обрабатывает новые посты из `posts.indexed`.

## 📋 Рекомендуемые улучшения (Best Practices)

### 1. Мониторинг и алерты

**Context7 Best Practice**: Добавить алерты для критических метрик.

**Задачи**:
- [ ] Добавить Grafana дашборды для:
  - Количество emerging/stable трендов
  - Распределение burst_score
  - Количество обработанных постов
  - Латентность обработки
- [ ] Настроить алерты:
  - Нет новых постов за 24 часа
  - trends_stable_task не запускается
  - Высокая латентность обработки (> 1s)

**Файлы**: `docs/TREND_PIPELINE_RUNBOOK.md`, `scripts/monitor_trend_activity.sh`

### 2. Улучшение логирования

**Context7 Best Practice**: Структурированное логирование с контекстом.

**Задачи**:
- [ ] Добавить логирование при переиспользовании кластеров:
  ```python
  logger.info("trend_worker_reused_existing_cluster", ...)
  ```
- [ ] Добавить логирование причин, почему тренды не стали stable:
  ```python
  logger.debug("trend_stable_task_skipped", 
               cluster_id=..., 
               reason="freq_long_too_low|source_diversity_too_low|burst_too_low")
  ```

**Файлы**: `api/worker/trends_worker.py`, `api/tasks/scheduler_tasks.py`

### 3. Оптимизация производительности

**Context7 Best Practice**: Оптимизация запросов и кэширование.

**Задачи**:
- [ ] Добавить индекс на `trend_clusters(label, status)` для быстрого поиска дубликатов
- [ ] Кэшировать результаты `_find_cluster_by_label` в Redis (TTL 5 минут)
- [ ] Оптимизировать запрос в `trends_stable_task` (добавить индексы на `trend_metrics`)

**Файлы**: `api/worker/trends_worker.py`, `api/tasks/scheduler_tasks.py`

### 4. Тестирование

**Context7 Best Practice**: Unit и интеграционные тесты.

**Задачи**:
- [ ] Добавить тесты для `_find_cluster_by_label`
- [ ] Добавить тесты для `_compute_burst` с разными значениями
- [ ] Добавить интеграционные тесты для объединения дубликатов

**Файлы**: `tests/unit/test_trends_worker.py`, `tests/integration/test_trend_detection.py`

### 5. Документация

**Context7 Best Practice**: Актуальная документация с примерами.

**Задачи**:
- [ ] Обновить `docs/TREND_DETECTION_ALGORITHM.md` с информацией о проверке дубликатов
- [ ] Добавить примеры использования скрипта `fix_trend_clusters.py`
- [ ] Документировать процесс стабилизации трендов

**Файлы**: `docs/TREND_DETECTION_ALGORITHM.md`, `docs/TREND_STABLE_EXPLANATION.md`

### 6. Улучшение качества трендов

**Context7 Best Practice**: Фильтрация шумных трендов.

**Задачи**:
- [ ] Добавить проверку на минимальное количество постов в кластере (>= 3)
- [ ] Улучшить фильтрацию generic labels (расширить список стоп-слов)
- [ ] Добавить проверку на временную активность (тренд должен быть активен минимум 2 часа)

**Файлы**: `api/worker/trends_worker.py`

### 7. Автоматизация исправлений

**Context7 Best Practice**: Периодическая очистка и оптимизация.

**Задачи**:
- [ ] Добавить периодическую задачу для объединения дубликатов (раз в день)
- [ ] Добавить задачу для архивации старых emerging трендов (> 7 дней без активности)
- [ ] Добавить задачу для пересчета burst_score для старых кластеров

**Файлы**: `api/tasks/scheduler_tasks.py`, `scripts/fix_trend_clusters.py`

## 🎯 Приоритеты

### Высокий приоритет
1. Проверка работы `trends_stable_task`
2. Проверка обработки новых постов
3. Мониторинг и алерты

### Средний приоритет
4. Оптимизация производительности
5. Улучшение логирования
6. Автоматизация исправлений

### Низкий приоритет
7. Тестирование
8. Документация
9. Улучшение качества трендов

## 📊 Текущее состояние

- **Emerging тренды**: 1137
- **Stable тренды**: 0
- **Активные за 24ч**: 99
- **Активные за 7д**: 653
- **Посты в trend_cluster_posts**: 107 (0 за последние 24ч)

## Следующие шаги

1. Проверить, что `trends_stable_task` работает корректно
2. Проверить, что новые посты обрабатываются
3. Настроить мониторинг и алерты
4. Оптимизировать производительность

