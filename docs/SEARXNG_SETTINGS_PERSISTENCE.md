# SearXNG - Сохранение settings.yml

**Дата**: 2025-02-02

## 🔍 Поведение SearXNG с settings.yml

### Важно: SearXNG НЕ перезаписывает settings.yml при рестарте

SearXNG **не перезаписывает** `settings.yml` если файл уже существует. Он создает файл только при **первом запуске**, если файл отсутствует.

### Проверка

1. **Файл монтируется как volume**:
   ```yaml
   volumes:
     - ./searxng:/etc/searxng
   ```

2. **SearXNG создает settings.yml только если файла нет**:
   - При первом запуске: создает дефолтный `settings.yml`
   - При последующих запусках: использует существующий файл

3. **Изменения сохраняются**:
   - Все изменения в `./searxng/settings.yml` на хосте сохраняются
   - При рестарте контейнера файл НЕ перезаписывается

### Рекомендации

1. **Всегда редактируйте файл на хосте**: `./searxng/settings.yml`
2. **После изменений перезапускайте контейнер**: `docker compose restart searxng`
3. **Проверяйте изменения внутри контейнера**: `docker compose exec searxng cat /etc/searxng/settings.yml`

### Если файл поврежден

Если `settings.yml` поврежден и SearXNG не запускается:

```bash
# Удалить файл (на хосте)
sudo rm /opt/telegram-assistant/searxng/settings.yml

# Пересоздать контейнер - SearXNG создаст новый дефолтный файл
docker compose --profile rag up -d --force-recreate searxng
```

**Внимание**: При этом все ваши кастомные настройки будут потеряны!

