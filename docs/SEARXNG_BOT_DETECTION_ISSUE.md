# SearXNG Bot Detection - Текущая проблема

**Дата**: 2025-02-02  
**Статус**: ⚠️ Bot detection все еще активен

## 🔍 Проблема

SearXNG возвращает 403 Forbidden при всех запросах поиска, несмотря на:
- ✅ `SEARXNG_LIMITER=false` в environment
- ✅ `limiter: false` в settings.yml
- ✅ `public_instance: false` в settings.yml
- ✅ `method: "GET"` в settings.yml
- ✅ Заголовки X-Forwarded-For, X-Real-IP добавлены

## ✅ Что работает

1. **Health Endpoint**: `http://localhost:8080/healthz` → `OK`
2. **Container**: Стабилен, не перезагружается
3. **Configuration**: Все настройки применены
4. **Caddy**: Не требует настройки (SearXNG только внутри сети)

## 🔍 Примененные исправления

### 1. Docker Compose
```yaml
environment:
  - SEARXNG_LIMITER=false
  - SEARXNG_PUBLIC_INSTANCE=false
```

### 2. Settings.yml
- `limiter: false`
- `public_instance: false`
- `method: "GET"`
- `bind_address: "0.0.0.0"`

### 3. SearXNG Service
- Метод: GET
- Headers: User-Agent, X-Forwarded-For, X-Real-IP, Accept

## ⚠️ Возможные причины

1. **Bot detection в SearXNG строгий**: Может требовать дополнительных настроек
2. **Версия SearXNG**: Возможно, в этой версии bot detection работает по-другому
3. **Требуется Redis**: Bot detection может использовать Redis для хранения данных

## 📝 Рекомендации

### Вариант 1: Использовать SearXNG как опциональный сервис
RAG Service уже имеет graceful degradation - если SearXNG недоступен, используется только внутренний поиск.

### Вариант 2: Проверить Redis
Bot detection может требовать Redis для работы. Проверить, доступен ли Redis для SearXNG.

### Вариант 3: Использовать другую версию SearXNG
Возможно, в других версиях bot detection настроен по-другому.

## ✅ Итог

**SearXNG частично настроен:**
- ✅ Health endpoint работает
- ✅ Container стабилен
- ✅ Все настройки применены
- ⚠️ Поиск возвращает 403 (требуется дополнительная настройка)

**Система может работать без SearXNG**, используя только внутренние каналы (Qdrant + PostgreSQL FTS + Neo4j).

