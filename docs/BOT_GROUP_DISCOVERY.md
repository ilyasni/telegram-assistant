# Telegram Bot — Подключение групп

## Цель
Добавить UX для подключения Telegram-групп через бота с учётом Context7 best practices и требований мультиарендности.

## Быстрый сценарий
1. **/groups** — показать текущие подключённые группы и меню действий.
2. **🔍 Найти новые группы** (кнопка) или команда **/group_discovery** — запустить discovery-запрос.
3. Дождаться пуш-уведомления с результатами или использовать **/groups_discovery_status**.
4. В списке результатов выбрать нужную группу (кнопка `➕ <Название>`) — бот отправит `POST /api/groups` и подключит чат.
5. Повторно открыть **/groups** — убедиться, что группа подключена.

## HTTP-вызовы бота
- `GET /api/users/{telegram_id}` → `tenant_id` + `user_id`
- `GET /api/groups?tenant_id=<uuid>` → список подключённых групп
- `POST /api/groups/discovery` → запуск discovery
- `GET /api/groups/discovery/{id}?tenant_id=<uuid>` → статус запроса
- `POST /api/groups` → регистрация группы в системе

## Проверка
- **Unit**: `pytest tests/unit/test_group_discovery.py`
- **Manual**:
  - `/groups` → отображается текст *«Группы не подключены»*.
  - `/group_discovery` → сообщение *«Запустил поиск групп»*, через ~5–10 секунд приходит список.
  - Кнопка `➕` для любой группы → сообщение *«Группа подключена»*, `/groups` показывает новую запись.

## Observability
- Логи aiogram (`bot.handlers.group_handlers`) фиксируют каждое обращение к API.
- Для диагностики FloodWait смотрим `telethon-ingest` (worker).

## Ограничения MVP
- Discovery берёт первые 12 групп из Telegram-диалогов.
- Подключение повторно вернёт `409`, бот интерпретирует это как успех.

