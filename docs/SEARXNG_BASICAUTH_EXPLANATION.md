# SearXNG BasicAuth - Объяснение

**Дата**: 2025-02-02

## ❓ Где взять SEARXNG_USER и SEARXNG_PASSWORD?

### Для локального SearXNG контейнера (по умолчанию)

**ОТВЕТ**: НЕ нужны! Оставьте пустыми.

```bash
SEARXNG_USER=
SEARXNG_PASSWORD=
```

**Причина**: Локальный SearXNG контейнер (`http://searxng:8080`) работает без аутентификации, только внутри Docker network.

### Для внешнего SearXNG инстанса (опционально)

Если вы используете внешний SearXNG с BasicAuth (например, `https://searxng.produman.studio`):

1. **Обратитесь к администратору инстанса**
   - Запросите учетные данные (username/password)
   - Или создайте учетную запись на инстансе

2. **Установите в `.env`**:
   ```bash
   SEARXNG_URL=https://searxng.produman.studio
   SEARXNG_USER=your-username
   SEARXNG_PASSWORD=your-password
   ```

3. **Перезапустите API**:
   ```bash
   docker compose restart api
   ```

## 📝 Текущая конфигурация

В проекте используется **локальный SearXNG контейнер**:
- ✅ URL: `http://searxng:8080`
- ✅ BasicAuth: НЕ требуется
- ✅ Network: Только внутри Docker network

## ✅ Проверка

```bash
# Проверка конфигурации
docker compose exec api python3 -c "
from config import settings
print(f'URL: {settings.searxng_url}')
print(f'User: {settings.searxng_user or \"(не задан - OK для локального)\"}')
print(f'Password: {\"***\" if settings.searxng_password else \"(не задан - OK для локального)\"}')
"
```

## 🔍 Когда нужен BasicAuth?

BasicAuth требуется только если:
- ✅ Используется внешний SearXNG инстанс
- ✅ Инстанс настроен с BasicAuth защитой
- ✅ Нужна аутентификация для доступа к API

**Для локального контейнера BasicAuth НЕ нужен!**

